# ODDM 认知树 - AI Agent 调用指南

本文档专供 AI 模型阅读，用于与认知树系统交互。读完即可调用，无需额外配置。

## 系统概述

认知树是一棵有生命的个人知识生态系统，不是静态笔记仓库。通过 CLI 命令交互，所有操作即时生效、内存存储。

**启动方式：** 在项目根目录执行 `node src/cli.js`，进入交互式命令行。

## 核心概念（调用前必读）

| 概念 | 说明 |
|------|------|
| **生/业/思** | 三大主干。生=生活日常，业=工作实践，思=想法构想 |
| **虚/实** | 节点状态。实=已验证的经验知识，虚=构想灵感待验证 |
| **热力** | 节点被关注程度。被注意(touch)则升，时间流逝(decay)则降 |
| **结晶** | 底层碎片积累3个以上子节点时，可升维为高阶原理 |
| **跨干关联** | 不同主干但有相同隐性标签(如"解耦")的节点自动建立关联 |
| **词条式** | 每个节点是一个知识单元。summary=提炼主旨(树上显示)，raw_source=完整原文 |

## 命令清单

### 快速通道（最常用）

```
note "内容"           存笔记（实节点，默认业主干）
note 生 "内容"        指定主干存笔记（生/业/思）
idea "内容"           存想法（虚节点，思主干）
```

### 查看与操作

```
list                  渲染认知树（干支叶层级，按热力排序）
view <node_id>        查看节点完整内容（含原文、标签、关联）
touch <node_id>       注意某个节点（热力上升）
decay                 全树自然衰减（模拟时间流逝）
```

### 结晶与关联

```
crystal               查看结晶提议（有3个以上子节点的底层碎片）
crystallize <id> "新表述"  执行结晶升维
cross                 重新发现跨干关联
```

### 种子与导出
```
seed [名称]            导入种子知识库（默认 oddm-knowledge）
parse "内容"           AI 解析对话/笔记为知识点（需配置 ARK_API_KEY）
export-md [文件名]     导出为 Markdown
export-html [文件名]   导出为 H5 SVG 页面
```

### AI 解析命令说明（parse）

`parse` 是白皮书 Phase 1「对话→JSON」解析层的 CLI 落地。工作流程：

1. 校验输入文本与 API Key（环境变量 `ARK_API_KEY`，火山方舟控制台获取）
2. 调用豆包 Chat Completions API（OpenAI 兼容，`https://ark.cn-beijing.volces.com/api/v3/chat/completions`），系统提示词为 `src/meta-prompt.js`（含三条红线与 Few-Shot 示例）
3. 返回结果经 `src/schema-validator.js` 校验（轴/状态/摘要/标签/红线3）
4. 展示解析结果，用户 `y` 确认写入 / `n` 放弃 / `e` 修改主干或状态后写入
5. 写入走 `KnowledgeTree.addFromAI()`：完整字段落库 + parent_hint 挂载提议 + 自动发现跨干关联

可配置环境变量：
- `ARK_API_KEY`：API Key（必填才能用 parse）
- `ARK_MODEL_ID`：模型 ID（默认 doubao-seed-2-1-pro-260628）
- `ARK_BASE_URL`：API 端点（默认北京区，一般无需改）

### 其他

```
add                   交互式添加节点（完整字段：主干/状态/主旨/标签）
inspect               全树自省快照（JSON）
help                  显示帮助
exit                  退出
```

## 节点 ID 说明

- 预置节点 ID：`work_oddm`、`work_oddm_path`、`work_oddm_ref`、`life_sleep`、`life_family`、`thought_tree`、`thought_axis`、`thought_heat`、`thought_crystal`
- 种子数据节点 ID：`oddm_*` 前缀（33个 ODDM 知识点）
- 快速通道生成的节点 ID：`note_xxx` 或 `idea_xxx`
- `list` 命令不显示 node_id（保持简洁），用 `view <node_id>` 查看详情

## 相关文档

- `DESIGN.md`：核心设计文档（产品定位、六条原则、数据模型、技术决策、Web 应用架构、AI 交互方案）
- `CHANGELOG.md`：代码变更日志
- `docs/vision-whitepaper.md`：Gemini 原设计文档（项目概念与愿景白皮书）
- `docs/meta-blueprint.md`：Meta-Blueprint 元规范（ODDM 对象数据契约、Meta-Prompt、渲染协议）
- `seed/oddm-knowledge.json`：33个 ODDM 知识点种子数据

## AI 典型工作流

### 工作流一：提炼标签

1. `list` 查看树，找到需要提炼标签的节点
2. `view <node_id>` 读取完整原文
3. 分析内容，提取显性标签（业务领域）和隐性标签（底层逻辑：复利/解耦/熵增/边界...）
4. 用 `add` 命令创建带标签的新节点，或建议用户更新现有节点标签

### 工作流二：扩展热度领域知识点

1. `list` 查看树，识别高热力节点（🔥多的）
2. `view <node_id>` 读取该节点完整内容
3. 基于该节点主题，生成相关的延伸知识点
4. 用 `note "延伸内容"` 或 `idea "延伸构想"` 存入
5. **AI 生成的知识点默认是虚节点或普通笔记，不自动标记为已验证**

### 工作流三：头脑风暴

1. `list` 查看现有树，理解用户当前关注领域
2. 基于用户的构想节点（思主干/虚节点），发散生成多个相关想法
3. 逐个用 `idea "想法内容"` 存入
4. 生成的想法全部是虚节点，等待用户后续实践验证

### 工作流四：对话知识沉淀

1. 处理完用户对话后，提炼出有认知增益的知识点
2. 判定主干：生活经验→生，工作实践→业，构想想法→思
3. 判定状态：已验证→实，待验证→虚
4. 提炼 summary（<30字）和隐性标签（1-3个底层逻辑词）
5. 用对应命令存入（推荐 `parse "原文"` 由 AI 自动提炼，或直接 `note`/`idea`）

### 工作流五：对话文本解析对齐（Few-Shot，与白皮书对齐）

AI 理解用户输入并提取结构时，遵循以下映射逻辑：

**示例 A（经验转化为实叶）：**
> 用户输入：*"今天调通了 Rust 的内存释放，发现只要严格遵守单播原则，根本不会产生内存泄漏。"*
> 理解结果：坐标 `业` | 状态 `实`（已被验证）| 显性标签 `[Rust, 内存管理]` | 隐性标签 `[所有权, 单播原则]`（底层抽象逻辑）

**示例 B（灵感转化为虚叶）：**
> 用户输入：*"突然想到，如果把团队分工也搞成微服务架构，每个人只暴露标准 API 接口，沟通成本会不会暴跌？"*
> 理解结果：坐标 `思` | 状态 `虚`（待验证假设）| 显性标签 `[团队管理]` | 隐性标签 `[解耦, 接口化, 边界]`（可与`业`架构设计联动）

## AI 行为约束（必须遵守）

1. **AI 生成的内容默认是虚节点**（构想/待验证），不得自动标记为实节点
2. **AI 不得自动执行结晶**（crystallize），只能用 `crystal` 查看提议并建议用户执行
3. **AI 不得自动删除或修改用户已有节点**，只能新增
4. **AI 扩展的知识点不主动推送**，存入后静默存在，由用户自行浏览
5. **隐性标签要提取底层逻辑**（如"解耦""复利""熵增""边界""涌现"），不是业务关键词
6. **每次操作后建议用 `list` 确认结果**
7. **推送代码前必须用户确认**：代码修改后先告知用户，等用户说"推送"再推送到 GitHub

## 三条行为红线（Anti-Patterns，必须遵守）

1. **禁止通用废纸桶**：绝不允许出现"其他""杂项""未分类"等兜底分类，任何节点必须强制锚定至【生/业/思】三轴之一
2. **禁止越权修改结构**：AI 仅能对"认知结晶升维"和"分支合并"生成提议（Proposal），必须保留接口等待用户显式确认，严禁在后台静默修改节点层级拓扑
3. **禁止隐性标签业务化**：隐性标签必须是跨领域通用的哲理或结构词（如复利/熵增/解耦），严禁将特定业务词（如 Python/今天吃的菜）打入隐性标签

## 输入输出示例

```
ctree> note "今天用ODDM重构了存储层，路径即拓扑真的很优雅"
📝 已存笔记 [业/实]: 今天用ODDM重构了存储层，路径即拓扑真的很优... [note_mto1xxx]

ctree> idea "认知树可以做成浏览器插件，右键就能存想法"
💡 已存想法 [思/虚]: 认知树可以做成浏览器插件，右键就能存想法 [idea_mto1xxx]

ctree> view work_oddm
─────────────────────────────────────────
📌 ODDM框架无图数据库设计
─────────────────────────────────────────
  ID:     work_oddm
  主干:   业 | 状态: 实 | 层级: L2
  热力:   2.5
  显标签: ODDM, 数据库
  隐标签: 解耦, 边界
  子节点: work_oddm_path, work_oddm_ref
  跨干:   life_family
─────────────────────────────────────────
  📄 完整内容:
  (原始对话或笔记原文)
─────────────────────────────────────────
```
