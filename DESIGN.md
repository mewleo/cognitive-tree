# ODDM 认知树 - 核心设计文档

## 产品定位

基于 ODDM（对象驱动数据库模型）构建的**个人认知生态系统**样板程序。不是传统的文件夹+笔记，而是一棵具有生命周期和呼吸感的"知识树"，通过与用户的日常对话自动提炼知识点，实现自生长。

## 六条核心原则

1. **生/业/思三主干**：所有知识挂载于三个正交主干——生（生活/生存）、业（工作/创造）、思（思考/情感）
2. **虚/实双态**：虚叶=灵感/未实现的理念（具有引力，主动寻找支撑）；实叶=已验证的知识和经验（树的基石）
3. **热力驱动**：放弃人工整理，通过热度自动呈现。高频关注=高亮放大（应季），长期不用=褪色折叠（落叶化土）
4. **认知结晶**：底层碎片积累到阈值，自动提议升维为第一性原理（替代复杂的落叶状态机）
5. **跨干启发**：AI 提炼隐性标签（如"复利""解耦"），在不同主干间建立关联，实现跨域启发
6. **主权在人**：系统自动整理/归纳/生长，但所有结构重大变更最终决定权在用户手中

## 数据模型（KnowledgeNode）

```
node_id       ODDM 对象唯一标识符
axis          生 | 业 | 思（三大主干）
state         虚 | 实（双态）
content       summary（AI提炼主旨，<50字）+ raw_source（原始内容指针）
tags          explicit（显性业务标签）+ implicit（隐性结构标签，用于跨界联想）
metrics       heat_score（热力值，随调用增加，随时间衰减）+ level（抽象层级，1=碎片，越大越接近第一性原理）
topology      parent_id + children_ids + cross_links（跨干关联）
```

## 技术决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 底层数据库 | ODDM（github.com/mewleo/oddm） | 用户已有项目，认知树作为 ODDM 样板应用，拆分而非合并 |
| 开发范式 | OOP + TDD | ODDM 是对象模型驱动，适合面向对象；测试驱动确保质量 |
| 技术栈 | Node.js 原生模块，零第三方依赖 | 大道至简，避免庞杂框架 |
| 交互形态 | CLI（当前）→ Web 应用（未来） | CLI 快速验证核心逻辑，Web 应用提供完整用户体验 |
| AI 集成 | Skill 优先于 MCP | 写好文档哪个模型都会调用，比 MCP 协议更有优势 |
| 渲染 | ASCII CLI + SVG H5 | 轻量，任意环境可预览 |

## Web 应用架构（规划中）

```
┌─────────────────────────────────┐
│  前端 (Web App)                  │
│  ├── SVG 知识树视图（可缩放/折叠） │
│  ├── 知识录入表单（手动/批量导入）  │
│  ├── 管理面板（节点编辑/层级设定） │
│  └── AI 对话面板（提炼/启发/评估） │
├─────────────────────────────────┤
│  后端 (Node.js API)              │
│  ├── REST API（CRUD/查询/导出）   │
│  ├── AI 适配器（默认豆包，可扩展） │
│  └── ODDM 数据层（SQLite 持久化） │
└─────────────────────────────────┘
```

### 导出定位
- `export-md`：完整 Markdown 文档（备份/分享）
- `export-html`：带完整内容的静态 HTML 快照（可离线浏览）
- SVG 图形树：Web 应用内的查看视图，不是导出物

### 部署场景
- Docker 镜像 + docker-compose（数据库卷挂载）
- 桌面端：Electron 包裹或本地启动 Node 服务

## AI 交互方案

| 能力 | 实现方式 |
|------|---------|
| **知识提炼** | 用户对话/文档 → AI 输出 KnowledgeNode JSON → 用户确认写入 |
| **偏好背书** | AI 回答前查知识树，有"实"节点的领域引用用户经验，虚节点标注"探索中" |
| **水平评估** | 基于 state×level×heat×children 生成领域掌握度分析 |
| **跨域启发** | implicit_tags 触发跨干关联，不同主干间提取同构经验 |
| **结晶提议** | 底层碎片积累到阈值，AI 提议升维，用户确认后执行 |

核心原则：**AI 只提议，人做决定**。

## 用户明确否决的方向

- 复杂标签体系（大道至简）
- 落叶倒计时状态机（用认知结晶替代）
- 网页应用版本（早期尝试，界面太复杂；后期规划为正式 Web 应用）
- MCP 协议（优先 CLI + 文档 + Skill）
- 放射状脑图布局（改为自上而下树状布局）
- 纯 SVG 图形作为导出物（SVG 是查看视图，导出用 MD/HTML）

## 与腾讯 ima 的区别

- ima：文档中心 + RAG 问答型
- 认知树：词条中心 + 自生长型，强调生命周期、热力驱动、跨域启发

## 与白皮书对齐（docs/vision-whitepaper.md）

> 白皮书是项目的最初愿景定义（Gemini 生成）。以下为概念对照、红线边界、路线图对齐。

### 概念对照

| 白皮书概念 | 当前实现 | 状态 |
|-----------|---------|------|
| 项目本质：ODDM 认知生态系统，有生命周期和呼吸感 | KnowledgeNode + KnowledgeTree，热力驱动自生长 | ✅ 已对齐 |
| 三主干（生/业/思），大道至简，拒绝无限层级 | axis 字段强制校验，只能生/业/思 | ✅ 已对齐 |
| 虚实双态：虚叶有引力主动寻找支撑，实叶是基石 | state 字段，虚叶通过 cross_links 关联实叶寻找支撑 | ✅ 已对齐 |
| 热力驱动：应季高亮放大，落叶化土但底层留存 | heat_score + touch/decay，渲染按热力排序；raw_source 永久留存 | ✅ 已对齐 |
| 隐性标签与跨界启发 | implicit_tags + discoverCrossLinks 自动跨干关联 | ✅ 已对齐 |
| 认知结晶：自动提议降维归纳为第一性原理 | suggestCrystallization（提议）+ crystallize（用户确认后执行） | ✅ 已对齐 |
| 主权在人：重大变更决定权在用户 | AI 只能提议，结构变更必须用户显式确认 | ✅ 已对齐 |

### 三条行为红线（Anti-Patterns，必须遵守）

1. **禁止通用废纸桶**：绝不允许"其他/杂项/未分类"等兜底分类。任何节点必须强制锚定至【生/业/思】三轴之一。
   - 落地：KnowledgeNode 构造函数校验 axis，非法值直接抛错；addNote 默认业但可指定，不存在"未分类"态。
2. **禁止越权修改结构**：AI 仅能对"认知结晶升维"和"分支合并"生成提议（Proposal），必须保留接口等待用户显式确认，严禁在后台静默修改节点层级拓扑。
   - 落地：crystallize 必须显式调用；AGENT.md 约束 AI 不得自动结晶/删除/修改。
3. **禁止隐性标签业务化**：隐性标签必须是跨领域通用的哲理或结构词（如复利/熵增/解耦），严禁将特定业务词（如 Python/今天吃的菜）打入隐性标签。
   - 落地：显性标签存业务词，隐性标签存底层逻辑；AGENT.md 已约束。

### 输入-输出对齐示例（Few-Shot）

AI 理解用户输入并提取结构时，遵循以下映射逻辑：

- **示例 A（经验→实叶）**：*"今天调通了 Rust 的内存释放，发现只要严格遵守单播原则，根本不会产生内存泄漏。"*
  → 坐标 `业` | 状态 `实` | 显性 `[Rust, 内存管理]` | 隐性 `[所有权, 单播原则]`
- **示例 B（灵感→虚叶）**：*"突然想到，如果把团队分工也搞成微服务架构，每个人只暴露标准 API 接口，沟通成本会不会暴跌？"*
  → 坐标 `思` | 状态 `虚` | 显性 `[团队管理]` | 隐性 `[解耦, 接口化, 边界]`

### 路线图对齐

| 白皮书 Phase | 内容 | 当前状态 |
|-------------|------|---------|
| Phase 1 | 数据解析与 Schema 校验（对话→标准 JSON） | ⏳ **未做**（下一步优先项） |
| Phase 2 | ODDM 对象持久化与查询（parent_id 提取树状拓扑） | ⏳ JSON 中间态，待接真实 ODDM |
| Phase 3 | 热力计算与 CLI 文本树（ASCII 树 + 🔥） | ✅ 已完成 |
| Phase 4 | 极简 SVG 动态渲染 | ✅ 已完成（**已偏离**：白皮书要求三干 120° 放射状，用户明确否决，改为自上而下树状布局） |

### 已知偏离（用户决策，非实现缺陷）

- 白皮书 Phase 4 要求"三干 120° 放射状 SVG"，用户实测后明确否决（节点密集、字体小、像脑图不像知识树），改为自上而下树状布局 + 矩形卡片。
- 白皮书以"对话文本"为系统输入（Phase 1 数据解析），当前 CLI 以"手动命令"为输入；下一步需补充 AI 对话解析层，两种输入并存。
