# ODDM 认知树 - 核心设计文档

## 产品定位

基于 ODDM（对象驱动数据库模型）构建的**个人认知生态系统**样板程序。不是传统的文件夹+笔记，而是一棵具有生命周期和呼吸感的"知识树"，通过与用户的日常对话自动提炼知识点，实现自生长。

## 六条核心原则

1. **生/业/思三主干**：所有知识挂载于三个正交主干——生（生活/生存）、业（工作/创造）、思（思考/情感）
2. **虚/实双态**：虚叶=灵感/未实现的理念（具有引力，主动寻找支撑）；实叶=已验证的知识和经验（树的基石）
3. **热力驱动**：放弃人工整理，通过热度自动呈现。高频关注=高亮放大（应季），长期不用=褪色折叠（落叶化土）
4. **认知结晶**：底层碎片积累到阈值，自动提议升维为第一性原理（替代复杂的落叶状态机）
5. **跨干启发**：AI 提炼隐性标签（如"复利""解耦"），在不同主干间建立关联，实现跨域启发
6. **主权在人**：系统自动整理/归纳/生长，但所有结构重大变更最终决定权在用户手中

## 数据模型（KnowledgeNode）

```
node_id       ODDM 对象唯一标识符
axis          生 | 业 | 思（三大主干）
state         虚 | 实（双态）
content       summary（AI提炼主旨，<50字）+ raw_source（原始内容指针）
tags          explicit（显性业务标签）+ implicit（隐性结构标签，用于跨界联想）
metrics       heat_score（热力值，随调用增加，随时间衰减）+ level（抽象层级，1=碎片，越大越接近第一性原理）
topology      parent_id + children_ids + cross_links（跨干关联）
```

## 技术决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 底层数据库 | ODDM（github.com/mewleo/oddm） | 用户已有项目，认知树作为 ODDM 样板应用，拆分而非合并 |
| 开发范式 | OOP + TDD | ODDM 是对象模型驱动，适合面向对象；测试驱动确保质量 |
| 技术栈 | Node.js 原生模块，零第三方依赖 | 大道至简，避免庞杂框架 |
| 交互形态 | CLI（当前）→ Web 应用（未来） | CLI 快速验证核心逻辑，Web 应用提供完整用户体验 |
| AI 集成 | Skill 优先于 MCP | 写好文档哪个模型都会调用，比 MCP 协议更有优势 |
| 渲染 | ASCII CLI + SVG H5 | 轻量，任意环境可预览 |

## Web 应用架构（规划中）

```
┌─────────────────────────────────┐
│  前端 (Web App)                  │
│  ├── SVG 知识树视图（可缩放/折叠） │
│  ├── 知识录入表单（手动/批量导入）  │
│  ├── 管理面板（节点编辑/层级设定） │
│  └── AI 对话面板（提炼/启发/评估） │
├─────────────────────────────────┤
│  后端 (Node.js API)              │
│  ├── REST API（CRUD/查询/导出）   │
│  ├── AI 适配器（默认豆包，可扩展） │
│  └── ODDM 数据层（SQLite 持久化） │
└─────────────────────────────────┘
```

### 导出定位
- `export-md`：完整 Markdown 文档（备份/分享）
- `export-html`：带完整内容的静态 HTML 快照（可离线浏览）
- SVG 图形树：Web 应用内的查看视图，不是导出物

### 部署场景
- Docker 镜像 + docker-compose（数据库卷挂载）
- 桌面端：Electron 包裹或本地启动 Node 服务

## AI 交互方案

| 能力 | 实现方式 |
|------|---------|
| **知识提炼** | 用户对话/文档 → AI 输出 KnowledgeNode JSON → 用户确认写入 |
| **偏好背书** | AI 回答前查知识树，有"实"节点的领域引用用户经验，虚节点标注"探索中" |
| **水平评估** | 基于 state×level×heat×children 生成领域掌握度分析 |
| **跨域启发** | implicit_tags 触发跨干关联，不同主干间提取同构经验 |
| **结晶提议** | 底层碎片积累到阈值，AI 提议升维，用户确认后执行 |

核心原则：**AI 只提议，人做决定**。

## 用户明确否决的方向

- 复杂标签体系（大道至简）
- 落叶倒计时状态机（用认知结晶替代）
- 网页应用版本（早期尝试，界面太复杂；后期规划为正式 Web 应用）
- MCP 协议（优先 CLI + 文档 + Skill）
- 放射状脑图布局（改为自上而下树状布局）
- 纯 SVG 图形作为导出物（SVG 是查看视图，导出用 MD/HTML）

## 与腾讯 ima 的区别

- ima：文档中心 + RAG 问答型
- 认知树：词条中心 + 自生长型，强调生命周期、热力驱动、跨域启发
